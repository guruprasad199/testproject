export interface CardData {
  heading: string;
  subtitleCombo: string;
  timings: string;
  caption: string;
  price: string;
  img: any;
}

export interface Testimonial {
  dp: string;
  content: string;
  name: string;
  organization: string;
}

export interface Slide {
  imageUrl: string;
}
